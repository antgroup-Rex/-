from umqtt.simple import MQTTClient# Test reception e.g. with:# mosquitto_sub -t foo_topic#  ipconfig# IPv4 Address. . . . . . . . . . . : 172.19.3.149'''def publish_data(server="172.19.3.149"):    c = MQTTClient("umqtt_client", server, port=9999)     c.connect()     c.publish(b"foo_topic", b"hello")    c.disconnect()if __name__ == "__main__":    publish_data()'''# def publish_data(server="iot.eclipse.org"):#     c = MQTTClient("myNewRan", server) #     c.connect() #     c.publish(b"myNewRan", b"hello")#     c.disconnect()# if __name__ == "__main__":#     publish_data()server="iot.eclipse.org"adc = machine.ADC(0)c = MQTTClient("myNewRan", server) c.connect()while True:  c.publish(b"myNewRan", str(adc.read()))