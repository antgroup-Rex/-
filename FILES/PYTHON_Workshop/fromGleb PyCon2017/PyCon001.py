'''# ESP8266# https://docs.micropython.org/en/latest/esp8266/esp8266_contents.html# pip install esptool# pip install network# pip install machine# pip install ampy1. need to flush the micropython Virtual machine on the chippython c:\programdata\anaconda2\lib\site-packages\esptool.py --baud 460800 --port COM4 write_flash --flash_size=detect  --flash_mode dio 0 esp8266-20170612-v1.9.1.bin2. install Puttyhttps://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html3. Via PuTTy connect to serial COM44.  wlan.connect("Gleb's Iphone","sxbjb9b367ok8")import networkwlan = network.WLAN(network.STA_IF) # create station interfacewlan.active(True)       # activate the interfacewlan.scan()             # scan for access pointswlan.isconnected()      # check if the station is connected to an APwlan.connect('essid', 'password') # connect to an AP5. MQTT Tutorial https://www.baldengineer.com/mqtt-tutorial.html'''import networkimport machinewlan = network.WLAN(network.STA_IF)wlan.active(True)# wlan.connect("Gleb's Iphone","sxbjb9b367ok8")wlan.connect('Macs-Labs',"macs2015")wlan.isconnected()# Not all pins are available to use, in most cases # only pins 0, 2, 4, 5, 12, 13, 14, 15, and 16 can be used.# Inner LED1pin = machine.Pin(2, machine.Pin.OUT, machine.Pin.PULL_UP)pin.value(1)pin.value(0)from machine import Pinp0 = Pin(0, Pin.IN)p2 = Pin(2, Pin.IN)# Microphone read:# https://tkkrlab.nl/wiki/Arduino_KY-038_Microphone_sound_sensor_module# to read the only analog pin:adc = machine.ADC(0)while True: 	adc.read()import umqtt